---
title: "Configurer SSL"
description: "Configurez HTTPS pour votre serveur rtSurvey. Requis avant de pouvoir vous connecter."
---

SSL doit être configuré avant de pouvoir vous connecter. Lorsque vous ouvrez l'application pour la première fois, vous serez automatiquement redirigé vers l'écran de configuration SSL.

---

## Options de configuration SSL

![Options de configuration SSL](/img/ssl-setup/ssl-setup-options.png)

Choisissez l'une des trois options :

| Option | Quand l'utiliser |
|--------|-------------|
| **Sous-domaine rtsurvey.com gratuit** *(Recommandé)* | Aucune configuration DNS requise. Nous créons l'enregistrement pour vous. Prêt en 2 à 5 minutes. |
| **Mon propre domaine** | Vous avez déjà un domaine et son DNS pointe vers ce serveur. |
| **Installer le certificat manuellement** | Entreprise ou CA personnalisé. Nécessite un accès SSH. |

---

## Option 1 — Sous-domaine rtsurvey.com gratuit *(Recommandé)*

C'est l'option la plus rapide. Aucune inscription de domaine ou modification DNS requise.

![Formulaire de configuration HTTPS](/img/ssl-setup/ssl-setup-form.png)

> **Avant de continuer :** Un sous-domaine gratuit nécessite un compte actif sur [licenses.rtsurvey.com](https://licenses.rtsurvey.com). Le plan gratuit inclut 1 sous-domaine (`*.rtsurvey.com`).

1. Cliquez sur **Sous-domaine rtsurvey.com gratuit** pour développer la section
2. Tapez le nom du sous-domaine souhaité dans le champ **VOTRE SOUS-DOMAINE**

   > Utilisez des lettres minuscules, des chiffres et des traits d'union. 3 à 30 caractères.
   > Exemple : `monprojet` → `monprojet.rtsurvey.com`

3. Collez votre **Jeton DNS rtSurvey** de [licenses.rtsurvey.com](https://licenses.rtsurvey.com) dans le champ jeton
4. Cliquez sur **Créer un sous-domaine sécurisé**

Une fois soumis, l'application commencera à provisionner votre certificat :

![Progression de la configuration HTTPS](/img/ssl-setup/ssl-setup-progress.png)

Les quatre étapes s'exécutent automatiquement :
- **Demande d'enregistrement DNS** — crée l'entrée DNS pour votre sous-domaine
- **Attente de la propagation DNS** — généralement 2 à 5 minutes
- **Émission du certificat SSL** — Let's Encrypt signe votre certificat
- **Activation du HTTPS** — Nginx redémarre avec le nouveau certificat

5. Une fois terminé, vous serez automatiquement redirigé vers votre nouvelle URL HTTPS.



---

## Option 2 — Mon propre domaine

Utilisez cette option si vous avez un domaine existant et que son enregistrement DNS `A` pointe déjà vers l'IP de ce serveur.

1. Cliquez sur **Mon propre domaine** pour développer la section
2. Entrez votre nom de domaine complet (par exemple `survey.myorganization.org`)
3. Cliquez sur **Créer un certificat**



Let's Encrypt vérifiera votre domaine et émettra un certificat. Cela nécessite que le DNS soit correctement pointé au préalable — sinon la demande échouera.

---

## Option 3 — Installer le certificat manuellement

Pour les environnements d'entreprise utilisant une CA personnalisée ou interne. Vous placerez vos fichiers de certificat sur le serveur via SSH, puis entrerez votre domaine dans l'application.

### Conditions préalables

- Accès SSH au serveur
- Un certificat valide et une clé privée pour votre domaine (format PEM)

### Étape 1 — Connexion SSH au serveur

```bash
ssh root@<server-ip>
```

### Étape 2 — Placer vos fichiers de certificat

Créez le répertoire et copiez vos fichiers :

```bash
mkdir -p /etc/letsencrypt/live/<votre-domaine>
```

Copiez vos fichiers dans ce répertoire avec ces noms exacts :

| Fichier | Description |
|------|-------------|
| `fullchain.pem` | Votre certificat + tous les certificats CA intermédiaires (concaténés) |
| `privkey.pem` | Votre clé privée |

Exemple :

```bash
# Copier depuis votre machine locale (exécuter ceci localement, pas sur le serveur)
scp fullchain.pem root@<server-ip>:/etc/letsencrypt/live/<votre-domaine>/fullchain.pem
scp privkey.pem  root@<server-ip>:/etc/letsencrypt/live/<votre-domaine>/privkey.pem
```

Définir les permissions correctes :

```bash
chmod 644 /etc/letsencrypt/live/<votre-domaine>/fullchain.pem
chmod 600 /etc/letsencrypt/live/<votre-domaine>/privkey.pem
```

### Étape 3 — Entrer votre domaine dans l'application



1. Dans l'écran de configuration SSL, cliquez sur **Installer le certificat manuellement**
2. Entrez votre nom de domaine (doit correspondre au Common Name ou SAN du certificat)
3. Cliquez sur **Appliquer**

Le serveur configurera Nginx avec votre certificat et redémarrera automatiquement.

---

## Étape suivante

Une fois SSL actif, passez à [Première connexion](first-login).
