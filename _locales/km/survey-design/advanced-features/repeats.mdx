---
title: "Advanced Repeats"
description: "Advanced patterns សម្រាប់ repeat groups: dynamic counts, nested repeats, summarising repeat data, និង referencing values across repeats។"
---

## ចំនួនការដដែលៗថាមវន្ត

| type | name | label | repeat_count |
|------|------|-------|--------------|
| begin_repeat | household_members | Household member | `${num_members}` |
| text | member_name | Member name | |
| integer | member_age | Age | |
| end_repeat | | | |

---

## ការចូលប្រើដោយលំដាប់: `indexed-repeat()`

| type | name | label | calculation |
|------|------|-------|-------------|
| calculate | first_name | | `indexed-repeat(${member_name}, ${household_members}, 1)` |

---

## ទីតាំងឧទាហរណ៍បច្ចុប្បន្ន: `index()`

| type | name | label |
|------|------|-------|
| begin_repeat | plots | Plot |
| note | plot_label | Plot number `${index()}` |
| end_repeat | | |

---

## ការសង្ខេបទិន្នន័យដដែលៗ

| Function | Example | ការពិពណ៌នា |
|----------|---------|-------------|
| `count(group)` | `count(${household_members})` | ចំ នួ ន instances |
| `sum(field)` | `sum(${loan_amount})` | Sum |
| `min(field)` | `min(${member_age})` | Minimum |
| `max(field)` | `max(${member_age})` | Maximum |
| `join(sep, field)` | `join(', ', ${member_name})` | Comma-separated list |
| `count-if(group, expr)` | `count-if(${members}, ${member_age} < 18)` | Conditional count |

---

## ការដដែលៗដំឡើងក្នុង

| type | name | label |
|------|------|-------|
| begin_repeat | households | Household |
| text | hh_id | Household ID |
| begin_repeat | hh_members | Member |
| text | member_name | Member name |
| end_repeat | | |
| end_repeat | | |

---

## វិធីល្អបំផុត

1. ប ្ រ ើ `repeat_count` ពេ ល ចំ នួ ន repetitions ត ្ រ ូ វ ប ា ន ដ ឹ ង ।
2. ដ ា ក ់ ឈ ្ ម ោ ះ repeat groups ដ ោ យ ច ា ស ់ ។
3. Test ជ ា ម ួ យ ចំ នួ ន instances ច ្ រ ើ ន ។

## ដែនកំណត់

- `indexed-repeat()` ត ្ រ ូ វ ក ា រ index valid ។
- Nested repeats ច ្ រ ើ ន ជ ា ង 2 levels ម ិ ន ណ ែ ន ា ំ ។
